[project]
name = "tata-ppe"
version = "0.1.0"
description = "Industrial PPE detection pipeline for CCTV/RTSP analytics (V1)"
requires-python = ">=3.10"
readme = "README.md"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
